import { View, Text } from "react-native";
import React from "react";

const Lesson: React.FC = ({ navigation, route }) => {
  return (
    <View>
      <Text>test</Text>
    </View>
  );
};

export default Lesson;
